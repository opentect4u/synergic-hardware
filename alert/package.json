{
  "name": "service-stock-alert",
  "version": "1.0.0",
  "private": true,
  "description": "Low new-device stock alerts for the Laravel service database",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "alert": "node server.js --run-once",
    "dry-run": "node server.js --run-once --dry-run"
  },
  "engines": {
    "node": ">=10.0.0"
  },
  "dependencies": {
    "aws-sdk": "^2.1692.0",
    "dotenv": "^8.6.0",
    "express": "^4.21.2",
    "mysql2": "^2.3.3"
  }
}
